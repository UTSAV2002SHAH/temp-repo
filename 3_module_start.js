// Modues importing 4,5,6

const names = require('./4_names')
const sayHi = require('./5_utils')

// in the file the function is called thus it will run
require("./6-granade");


//console.log(names)
// sayHi('bobby');
// sayHi(names.john);
// sayHi(names.sam);
